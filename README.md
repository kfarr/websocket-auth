# websocket-auth
Experiment to connect websocket with an authentication mechanism

## How to
  git clone
  npm install
  npm start
  point browser to 127.0.0.1:3000 # (do NOT go to localhost:3000, it won't work through twitter oauth process)
